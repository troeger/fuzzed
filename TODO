- Tweak settings.py on Puppet execution, e.g. by creating a settings.local.py

- Configure PostgreSQL database on production server with Puppet
- Work on Windows installation of dev machine
- Pre-test Linux installation of production machine
- Configure init script for rendering service (via puppet service resource)
- Needs fixed symbolic link in TAR file so that initscript dest path remains the same



