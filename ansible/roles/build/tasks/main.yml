---
- name: Ensure SCONS is installed
  sudo: yes
  apt: pkg=scons state=present
  when: ansible_os_family == "Debian"

- name: Ensure SCONS is installed
  homebrew: pkg=scons state=present
  when: ansible_os_family == "Darwin"

- include: gcc.yml
- include: lessc.yml
- include: node.yml
- include: ../../_lib/xml.yml
- include: ../../_lib/boost.yml
