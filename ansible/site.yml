---
- name: Configure production web servers
  hosts: webservers
  user: root

  roles:
    - web
    - web_install

- name: Configure production backend servers
  hosts: backendservers
  user: root

  roles:
    - backend
    - backend_install

- name: Configure developer machine
  hosts: devmachine
  user: root

  roles:
    - build
    - web
    - backend
    - testing

- name: Configure Vagrant developer machine
  hosts: vagrantmachine
  user: root

  roles:
    - build
    - web
    - backend
    - testing
    - env_vagrant
