---
- name: Configure developer machine for frontend
  hosts: localhost
  user: root

  roles:
    - all
    - build_fuzzed
    - build_less_code
    - build_xsd_code
    - run_fuzzed_fe
    - test_fuzzed
